using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Healper : MonoBehaviour
{
    bool f1;
    float f2;
    int f3;
    Vector3 f4;

    // Start is called before the first frame update
    void Start()
    {
        int a = 100;
        while (a > 0)
        {
            //Debug.Log(a);
            a = a - 1;
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
